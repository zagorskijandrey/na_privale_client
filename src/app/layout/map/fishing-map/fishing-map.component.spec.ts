import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FishingMapComponent } from './fishing-map.component';

describe('FishingMapComponent', () => {
  // let component: FishingMapComponent;
  // let fixture: ComponentFixture<FishingMapComponent>;
  //
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ FishingMapComponent ]
  //   })
  //   .compileComponents();
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(FishingMapComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should be created', () => {
  //   expect(component).toBeTruthy();
  // });
});
