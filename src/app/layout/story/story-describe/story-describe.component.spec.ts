import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { StoryDescribeComponent } from './story-describe.component';

describe('StoryDescribeComponent', () => {
  // let component: StoryDescribeComponent;
  // let fixture: ComponentFixture<StoryDescribeComponent>;
  //
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ StoryDescribeComponent ]
  //   })
  //   .compileComponents();
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(StoryDescribeComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should be created', () => {
  //   expect(component).toBeTruthy();
  // });
});
